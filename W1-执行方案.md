# W1 执行方案（9/19–9/25）

> 配套 `PLAN-2026-10-Go求职冲刺.md` §7.1。
> **节奏：每天基准 6h（上午 2.5h 编码 / 下午 2h 知识 / 傍晚 1h 投递 / 收尾 0.5h）｜状态好可到 8h，不设硬日额｜允许减量但不归零。**
> 用法：勾掉「验收标准」才算过关；每天收尾填 §3 一行。

---

## 0. W1 时间账

| 序 | 日期 | 星期 | 投入 | 定位 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 9/19 | 六 | ~7h | 环境就绪 + 简历启动 + P0-① |
| — | 9/20 | 日 | ≤2h | **休息为主**；可选写简历初稿 |
| 2 | 9/21 | 一 | ~6h | 简历定稿 + 项目骨架 |
| 3 | 9/22 | 二 | ~6h | 🎯 **投递日**：Go 练手批 + PHP 试水 |
| 4 | 9/23 | 三 | ~6h | 数据层打通 |
| 5 | 9/24 | 四 | ~6h | 测试 + 并发入门 |
| 6 | 9/25 | 五 | ~6h | 🎯 P0 收尾 + **Go 正式第一批投递** |

**W1 目标（可验证）**
1. Go 环境在 WSL 内可用，代码在 `~/go-learning`，能推 GitHub
2. **简历定稿 v1 + Go 练手批 3–5 家 + PHP 岗 3–5 家已投**
3. 项目骨架能跑：`/health` + 一个示例接口 + 统一响应/错误码
4. Docker 能起 MySQL + Redis
5. **P0 完成 7 项**（切片/map/interface/error/defer/并发/context）

> **Go 投递从 9/26（W2 初）启动** —— P0 学完后开始，正式第一批须赶在 9/30 前投完。

---

## 1. 逐日执行

### 9/19（周六）— **简历启动日**（环境已完成，全天重心转向简历）

> **9/18 实际完成**：环境四项全部就绪（Go/Docker/SSH/迁移）+ P0-① 切片。
> 因此 9/19 只剩 2 件事：**简历时间线**（关键路径）+ **JD 拆解**（决定简历怎么写）。

| 时段 | 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- | :--- |
| 上午 | **① 简历：列工作经历时间线**（2h） | 一张表：`公司｜起止｜岗位｜核心系统｜我负责什么｜量化结果`，覆盖全部 5 年 | ⬜ |
| 上午 | **② 拆 5–10 份上海 Go 岗 JD**（1.5h） | **词频表**：出现 ≥5 次的技能/要求排序 + 记录硬性门槛（年限/微服务/学历） | ⬜ |
| 下午 | **③ 简历初稿**（2h） | 按 ① 的时间线 + ② 的词频表成文，**当晚发我审查** | ⬜ |
| 傍晚 | ④ Git 仓库初始化（15min） | `~/go-learning` 或新项目目录 `git init`，`.gitignore` 排除 `.gocache`，**推到 GitHub** | ⬜ |
| 傍晚 | ⑤ P0-① 切片：补 90 秒自述 | 录音一遍，能不看稿讲「值传递 / 底层数组共享 / 扩容规则」 | ⬜ |

**明天为什么是简历日**：

- 9/22 要投 **Go 练手批 + PHP 试水**，简历是**唯一前置条件**
- ③ 的词频表直接决定简历的**技能栈排序**和**项目描述重点**
- ① 的时间线不先做，③ 会写成流水账（5 年经验的人写简历，难的从来不是措辞，是**回忆出可量化的成果**）

**做不完时的优先级：① > ③ > ② > ④ > ⑤**
（时间线必须先有；JD 拆解可以与初稿并行补）

#### ② 的详细方法：JD 怎么选、怎么拆

**搜索关键词**（BOSS 直聘，都试一遍）：
```
Go 后端开发 / Golang 后端 / Go 服务端 / 服务端开发 Go / Go PHP
```
筛选：城市 **上海**；经验 **3–5 年** 与 **5–10 年** 各看一批。

**抽样必须分层**（否则词频表失真）：

| 类型 | 数量 | 特征 | 作用 |
| :--- | :--- | :--- | :--- |
| 主力目标 | 4–5 | 5 年经验、技能栈含 Go、业务对口（电商/交易/中台） | 决定简历**主打方向** |
| 冲刺目标 | 2–3 | 心仪公司 / 要求略高（微服务、K8s） | 决定**加分项**该补什么 |
| 保底 | 2–3 | 中小公司 / **Go-PHP 混合栈** / 门槛宽松 | 确保有面试机会 |

**排除**：要求 Go 专职 3 年以上的、架构师/Leader 岗、JD 不含技术栈的。

**逐份提取 4 类信息**：

| # | 公司 | 类型 | 硬性门槛（年限/学历/是否要微服务） | 技术栈 | 业务领域 | 软性要求 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | | 主力/冲刺/保底 | | | | |
| 2 | | | | | | |
| 3 | | | | | | |
| … | | | | | | |

**汇总词频表**（出现 ≥5 次的，就是简历必须有的关键词）：

| 关键词 | 出现次数 | 归类 | 你目前有没有 | 要不要补 |
| :--- | :--- | :--- | :--- | :--- |
| 例：Gin | 8 | 框架 | ✅ 有（W1 项目会用） | 已有 |
| 例：Kafka | 6 | 中间件 | ✅ 有（W3 项目会用） | 已有 |
| 例：K8s | 5 | 云原生 | ❌ 无 | 只背概念（方案已裁） |

> **⏰ 时间盒**：整个 ② 控制在 **1.5h**。
> 拆够 10 份就停，**不要为了「更全面」看 50 份** —— 那是调研瘫痪，不是准备。

---

### 9/20（周日）— 休息为主 + 简历初稿（≤2h）

| 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- |
| **完全休息**（不碰代码、不看八股） | —— | ⬜ |
| *可选*：写完简历初稿（≤2h） | 初稿成文，**当晚发我做审查** | ⬜ |

> **为什么破了「周日全休」的例**：简历是关键路径（9/22 必须投出）。
> 若初稿压到 9/21，那天要同时完成「写初稿 + 按审查意见改 + 定稿 + 项目骨架」——**一天装不下**。
> 所以把「写初稿」这个**创造性但轻量**的 2h 挪到周日：不影响体力恢复，但能救回整条投递节奏。
> **如果你确实需要完全放空，就推迟到 9/21 上午，代价是 9/21 会很赶。**

---

### 9/21（周一）— 简历定稿 + 项目骨架

| 时段 | 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- | :--- |
| 上午 | 按我的审查意见改简历 | **简历定稿 v1**（能直接投） | ⬜ |
| 上午 | 整理目标公司清单（练手 3–5 家 + 心仪 10 家） | 台账建好，含各家 JD 关键要求 | ⬜ |
| 下午 | 建仓库 + 分层目录（handler/service/repository/model） | 目录定稿，README 初始化，**推到 GitHub** | ⬜ |
| 下午 | Gin 起 HTTP 服务 + 统一响应体 + 统一错误码 | curl 通 `/health` 和一个示例接口 | ⬜ |
| 傍晚 | 读 **P0-② map** | 2 个 demo，能讲清「为什么并发写 panic」 | ⬜ |
| 傍晚 | 装 Docker Desktop（若已下完） | `docker run --rm hello-world` 成功 | ⬜ |

---

### 9/22（周二）— **投递日** 🎯（Go 练手批 + PHP 试水）

> **双线并行**：Go 按原计划投练手批（拿市场数据）；PHP 顺手投几家（**找面试感觉 + 探市场价**）。
> 两者共用傍晚的 1h 投递时段，**不需要额外时间**。

| 时段 | 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- | :--- |
| 上午 | 简历准备两个版本：Go 版 + PHP 版（**PHP 版保留 Go 项目经历**） | 两版都能直接投 | ⬜ |
| 上午 | **投出 3–5 家 Go 练手公司**（外包 / 不心仪的） | 台账 ≥3 条，状态=已投 | ⬜ |
| 上午 | **投出 3–5 家 PHP/Laravel 岗**（优先中大型公司） | 台账 ≥3 条 | ⬜ |
| 下午 | Docker Compose 起 MySQL + Redis | `up -d` 后 3306/6379 有监听 | ⬜ |
| 下午 | 读 **P0-③ interface** | 3 个 demo，讲清 `(*T)(nil)` 装进 interface 后 `!= nil` 的坑 | ⬜ |
| 傍晚 | 口述练习：讲一遍项目分层设计 | 录音 3 分钟，不看稿 | ⬜ |

> **Go 练手批的目的不是拿 offer，是拿数据**：简历能不能过筛？面试官问什么？
> **被拒也是有效信息** —— 拒你的理由就是你要补的清单。
>
> **PHP 投递的两个目的**：① 找面试感觉（内容你已熟，成本极低）② **探真实市场价**。
> 若 PHP 能拿到 25K，那你在 Go 岗报 18–20K 就是主动损失。
> **⚠️ 心理陷阱**：拿到 PHP offer 后容易产生「就这样吧」而放弃 Go 转型 —— 提前意识到它。

---

### 9/23（周三）— 数据层打通

| 时段 | 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- | :--- |
| 上午 | GORM 连 MySQL，建 商品/库存/订单 三张表 | 表结构落库，能插入查询 | ⬜ |
| 上午 | 写第一个 repository（返回 `(data, error)`） | **无 message 字符串** | ⬜ |
| 下午 | 读 **P0-④ error**，**立刻改造上面的 repository** | 错误用 `%w` 包装；讲清 `errors.Is/As`、错误分层、panic 边界 | ⬜ |
| 下午 | 把 repository 抽成 interface，service 依赖接口 | 讲清「为什么面向接口」 | ⬜ |
| 傍晚 | 投递跟进：查 Go 练手批 + PHP 岗回复状态 | 台账更新；有邀约立刻排期 | ⬜ |

> **「学完就用」的样板**：下午学 error，当天就改自己的代码 —— 这是 204h 能撑住的唯一方式。

---

### 9/24（周四）— 测试 + 并发入门

| 时段 | 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- | :--- |
| 上午 | 写 3 个单元测试（repository / service 覆盖） | `go test ./...` 全绿 | ⬜ |
| 上午 | 跑 `go test -race` | **无 race 告警** | ⬜ |
| 下午 | 读 **P0-⑤ defer** | 讲清执行顺序、与 return 的交互、循环里的陷阱 | ⬜ |
| 下午 | 读 **P0-⑥ 并发（上）**：goroutine + channel | 3 个 demo：无缓冲/有缓冲、关闭语义、range | ⬜ |
| 傍晚 | 简历微调（按 PHP 面试反馈）+ 口述练习 | —— | ⬜ |

---

### 9/25（周五）— P0 收尾 + 验收 + **Go 正式第一批投递** 🎯

| 时段 | 任务 | 验收标准 | 状态 |
| :--- | :--- | :--- | :--- |
| 上午 | 读 **P0-⑥ 并发（下）**：select + sync | `select` + `ctx.Done()` demo；WaitGroup/Mutex/Once 用法 | ⬜ |
| 上午 | 读 **P0-⑦ context** | 讲清取消传播、超时、为什么是「协作式取消」 | ⬜ |
| 下午 | **投出 Go 正式第一批 8–10 家**（含心仪公司） | 台账更新，含匹配度自评 | ⬜ |
| 下午 | 3 分钟自述录音：分层 + 选型理由 | 不看稿能讲完 | ⬜ |
| 傍晚 | **把骨架代码 / GitHub 链接发我审查** | —— | ⬜ |

> **正式第一批尽量赶在国庆前投完** —— 10/1–10/4 是国庆 + 周日，HR 休假、反馈停滞。
> 若 9/25 投不完，10/1–10/3 继续补投（投递不亏，只是反馈会延迟）。

---

## 2. 每日时段分配（6h 基准）

| 时段 | 时长 | 内容 |
| :--- | :--- | :--- |
| 上午 | 2.5h | 项目编码（骨架 → 数据层） |
| 下午 | 2h | P0 知识点 + demo（**结论式笔记，禁止摘抄**） |
| 傍晚 | 1h | 简历 / JD / 投递 / 口述练习 |
| 收尾 | 0.5h | 填 §3 + 写下明天的第一个动作 |

**纪律**：P0 每个知识点必须**写出可运行 demo + 3 句自己的结论**，并尽量当天在项目里用上。
你的 `notes/` 是摘抄式的且停在 8/24 —— **换方法比补进度重要**。

---

## 3. 每日收尾记录

| 日期 | 完成 | 卡点 | 明天的第一个动作 |
| :--- | :--- | :--- | :--- |
| 09-18 | **环境全部就绪**：C 盘 25→42.8GB；WSL2+Ubuntu@D盘；WSL 内 Go 1.26.5；代码迁 `~/go-learning`；GitHub SSH；Docker 4.91+镜像加速；**P0-① 切片**（含源码级验证） | —— | 简历工作经历时间线 |
| 09-19 | | | |
| 09-20 | | | |
| 09-21 | | | |
| 09-22 | | | |
| 09-23 | | | |
| 09-24 | | | |
| 09-25 | | | |

---

## 4. W1 结束时的自检（9/25 逐条打勾）

- [ ] `go version` 在 WSL 内可用；代码在 `~/go-learning` 且已推 GitHub
- [ ] `docker run --rm hello-world` 成功；Compose 能起 MySQL + Redis
- [ ] 项目骨架：`/health` + 示例接口 + 统一响应体/错误码，**已推 GitHub**
- [ ] 简历定稿 v1（Go 版 + PHP 版两版）
- [ ] **Go 练手批 3–5 家 + PHP 岗 3–5 家已投**；Go 正式第一批 8–10 家已投
- [ ] P0 七项各有可运行 demo + 结论笔记
- [ ] `go test -race` 无告警
- [ ] 录过一次 3 分钟自述，不看稿能讲完

---

## 5. 简历自检清单

### 5.1 致命项（有一条就会被 Go 岗筛掉）

- [ ] 简历里**没有**任何 Go 关键词 → 直接出局
- [ ] 只写「了解 Go / 正在学习 Go」→ 等于没有，必须有**可验证产出**
- [ ] 5 年经验但简历看起来像 1 年（全是「负责 XX 模块开发」，无量化、无主导）
- [ ] 期望薪资写成 18–20K → **建议不写**，或写「面议」

### 5.2 加分项

- [ ] 标题写「**高级后端工程师（Go / PHP）**」，不是「PHP 工程师」
- [ ] 技能栈列表 **Go 排第一**
- [ ] 有 Go 项目且带 **GitHub 链接**（面试官真的会点）
- [ ] 项目描述里有**取舍**（如「Kafka 无延迟队列，故用轮询兜底」）—— 5 年经验的标志
- [ ] 有**量化数字**：QPS、P99、数据量、并发量、优化前后对比
- [ ] 电商 / 订单 / 库存 / 支付经验被**突出**（你最值钱的部分）
- [ ] 主动提到**一致性 / 幂等 / 事务边界**（体现工程判断力）

### 5.3 项目表述模板（项目未完成时也能投）

> **Go 电商交易链路服务**（个人项目 / 2026.09–至今）[GitHub]
> 技术栈：Go 1.26 · Gin · GORM/MySQL · Redis · Kafka · gRPC · Docker Compose
> - 基于 Redis + Lua 实现秒杀库存原子预扣，压测下 0 超卖，QPS ___ / P99 ___ms
> - 用 Kafka 削峰异步创建订单，消费端幂等（唯一约束 + 状态机）保证不重复扣减
> - 订单超时关单：因 Kafka 无原生延迟队列，采用延迟 topic + 定时轮询兜底，
>   代价是重复投递开销，收益是无需引入额外中间件
> - 将库存服务拆为独立 gRPC 服务，etcd 服务发现，含超时/重试与 traceID 透传

**诚实原则**：没做完的写「进行中」，数字没压测出来就留空。
**绝不编造** —— 面试官会顺着数字追问细节，编的数字撑不过三个问题。

### 5.4 空窗期（不写进简历，只在被问时回答）

> 「刚结束上一段，计划集中把 Go 技术栈补齐。目前已完成一个电商交易链路的 Go 项目，
> 包含订单与库存一致性、秒杀削峰，以及一次真实的微服务拆分。」

---

## 6. 投递台账模板

| 公司 | 岗位 | JD 关键要求 | 投递日 | 状态 | 面试问题 | 失分点 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| | | | | 已投/已读/邀面/一面/二面/offer/拒 | | |

**渠道优先级**：内推 > BOSS 直聊 > 猎聘 > 官网 > V2EX

**PHP 投递选公司的标准**：正常招聘的 PHP/Laravel 岗，**优先中大型公司**——
目的是拿到真实的市场价锚点，所以不要投明显压价的小作坊（那种数据没参考价值）。

**Go 投递（9/26 起）选公司的标准**：练手批性质不再单列，直接从「门槛适中 + 非最优选」
的公司开始投，把最心仪的 3–5 家留到 10 月初投（那时项目已到 W3、状态更好）。
