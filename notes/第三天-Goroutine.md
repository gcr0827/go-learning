1. channel

   ```go
   # 数据沿着箭头的方向流动
   ch <- v    // 将 v 发送到通道 ch。
   v := <-ch  // 从 ch 接收，并将值赋给 v。
   ```

   1. Channel 是引用类型，但**零值是 `nil`**。一个 `nil` channel 既不能发送也不能接收，且**会永远阻塞*
   2. **对已关闭且为空的通道执行接收操作都不会阻塞**
   3. range 接收协程时需要发送者主动关闭协程，不然会导致死锁