# Go 求职冲刺方案（2026-09-18 → 2026-10-31）

> 已通过 grill-me 逐项确认。技术栈 W1 定稿后不再更换。
> 每日收尾时在「§8 实际进度」写一行，这是我做模拟面试和纠偏的输入。

---

## 1. 约束与决策记录

### 1.1 硬约束（事实）

| 项 | 值 | 影响 |
| :--- | :--- | :--- |
| 今天 | 2026-09-18（周五） | 可支配 **43 天 / 6 周** |
| 目标 | **10 月底签约** | 不是「开始投递」，是拿到 offer |
| 状态 | 已离职 **10 天**，可全职 | **每天基准 6h（可持续）**；有余量再补新知识点；周日全休 |
| 市场 | 上海 | Go 主投，PHP 保底 |
| 年限 | 5 年+，PHP/Laravel | 定位「高级后端」，非「Go 资深」 |
| 存量 | 电商交易类 + SaaS/CRM | 项目题材：电商交易链路 |
| 本地环境 | Windows: Go **1.26.5**、Git 2.51.0、GOPROXY=goproxy.cn | ✅ 可用 |
| 本地环境 | **WSL2 + Ubuntu @ `D:\DevSoft\WSL\Ubuntu`**（发行版 2，账号已设） | ✅ 可用 |
| 本地环境 | WSL 内 Go | ⬜ 待装（见 `环境搭建-记录.md`） |
| 本地环境 | **Docker Desktop 未安装**，3306/6379/9092 无监听 | ⚠️ 阻塞 W2，必须 W1 完成 |
| 磁盘 | C: 42.8 GB 可用 / D: 682 GB 可用 | ✅ 已清理并重分配 |

### 1.2 已决策事项（不再反复）

| 决策 | 选择 | 理由 |
| :--- | :--- | :--- |
| 项目形态 | 单仓库 + **一次真实拆分** | 6 周内能讲透的微服务上限；多服务脚手架会崩 |
| MQ | **Kafka** | 一次到位不折腾；代价：无原生延迟队列，需自实现 |
| 框架 | **Gin**（不用 kratos/go-zero） | 分层透明可控，避免被 DI/Wire 原理追问击穿 |
| 部署 | **Docker Compose**，不做 K8s | K8s 是最大时间黑洞，W5 只背核心概念 |
| 算法 | **暂不做**（见 §7 风险 R4，建议降级保留） | 用户决定，已知代价 |
| PHP 线 | 同步投递，**不补新知识**，仅做面试复盘 | 保底，不稀释主线 |
| 薪资 | 期望 18–20K（**建议前两周不提数字**，见 §5） | 低报不提高通过率，只会压死自己 |
| 开发环境 | **主力 WSL**，代码放 `~/go-learning`（WSL 文件系统内） | Linux = 生产环境，编译快 5–10 倍，`-race`/压测更准 |
| 简历 | **单份 Go 优先**，工作经历如实写 PHP | 维护成本最低，迭代快 |

---

## 2. 定位与话术

### 2.1 简历定位

> **高级后端工程师（Go / PHP）**
> 5 年交易与 SaaS 系统开发经验，主导订单、支付、库存一致性等核心链路；
> 现以 Go 为主力技术栈，能独立完成单体服务设计与微服务拆分。

**不要自称「Go 资深」**——那会被用 Go 资深的标准追问，答不上来比不加分更伤。

### 2.2 你的真实优势（面试要主动讲，别等人问）

Laravel 的工程习惯和 Go 的审美高度重合，这是**可迁移的方法论**：

| 你在 Laravel 里的做法 | Go 里的对应 |
| :--- | :--- |
| Controller 薄层，业务下沉 Model/Biz | handler 薄层，逻辑进 service |
| FormRequest 前置校验 + 库查询辅助 | 参数绑定 + 校验中间件 |
| Model 返回纯数据，不返回 message | repository 返回 `(data, error)` |
| 事务边界、幂等、缓存一致性 | 概念完全通用，只换实现 |

**「为什么从 PHP 转 Go」标准答法**：
> 「PHP 让我理解了业务和分层，但它的运行模型限制了我在并发和性能上的判断力。
> 我做过订单和库存，最痛的就是超卖和一致性——这正好是 Go 的主场。
> 我的分层习惯、事务边界意识、幂等设计可以直接迁移，只是换了语言和并发原语。」

### 2.3 空窗期（这是你的牌，不是风险）

离职仅 10 天，**不需要解释**。被问到就说：

> 「刚结束上一段，计划集中把 Go 技术栈补齐。目前已经完成一个电商交易链路的 Go 项目，
> 包含订单与库存一致性、秒杀削峰、以及一次真实的微服务拆分。」

---

## 3. W1–W2：Go 地基（有优先级，不平均用力）

面试题分布是 **知识点要广度、并发与项目要深度、Runtime 只要「能讲结论」**。
所以补底顺序按「面试频率 × 项目依赖度」排，**不按教材章节排**。

### P0 — 必须吃透到「能写、能讲、能画图」

1. **切片**：底层数组 / len / cap、append 扩容策略、共享底层数组的坑、传参的拷贝语义
2. **map**：底层结构、扩容、**为什么并发写会 panic**、sync.Map 适用场景
3. **interface**：鸭子类型、类型断言与 type switch、`(*T)(nil)` 装进 interface 后 `!= nil` 的经典坑
4. **error**：`errors.Is/As`、`%w` 包装链、错误分层（业务错 vs 系统错）、panic/recover 边界
5. **defer**：执行顺序、与 return 的交互、循环里的性能陷阱
6. **并发**：goroutine 生命周期、channel（有/无缓冲、关闭语义、range）、`select` + `ctx.Done()`、`sync.Mutex/RWMutex/Once/WaitGroup`、`errgroup`
7. **context**：取消传播、超时、为什么是「协作式取消」、在 HTTP 链路里的位置

> 你现有的 `day1`~`day4` 与 `notes/` 只覆盖 channel/goroutine/context 入门，且**已停 25 天**。
> **建议重做**：不再摘抄笔记，改成「每个知识点写一个可运行 demo + 3 句自己的结论」。

### P1 — 跟着项目学，不单独开课

8. `net/http` / Gin：中间件顺序与原理、路由分组、统一响应与错误码、请求超时、优雅关闭
9. 工程化：`go mod`、目录分层、配置（viper/env）、日志（zap/slog）、单测 + `-race` + benchmark
10. 数据层：`database/sql` 连接池、GORM 选型与陷阱、**事务显式传递**、go-redis
11. Kafka 客户端（sarama/segmentio）：生产者、消费者组、offset 提交、幂等消费

### P2 — 只到「能讲结论和取舍」，**不死磕源码**

12. **GMP**：为什么需要 GMP、work stealing、GOMAXPROCS 的影响
13. **GC**：三色标记 + 写屏障、STW 现状、如何减少对象分配
14. **内存**：逃逸分析结论、栈 vs 堆、`sync.Pool` 适用场景

> P2 策略是**保底应答**：问起能答出层次和影响因素即可。6 周内死磕 Runtime 源码投入产出比最差。

---

## 4. 项目：电商交易链路

### 4.1 技术栈（定稿）

```
Go 1.26 · Gin · GORM + MySQL · Redis · Kafka · gRPC + etcd · zap · Docker Compose
```

### 4.2 必须做出来的能力点（**204h 预算下的优先级**）

🔥 = 保住不砍 ｜ ⚠️ = 降级实现 ｜ ❌ = 放弃（只背八股）

| 能力点 | 对应面试题 | 阶段 | 204h 预算 |
| :--- | :--- | :--- | :--- |
| 下单：事务 + 库存扣减 | 事务边界、锁的范围、如何避免超卖 | W2 | 🔥 完整 |
| 乐观锁 / 唯一约束防重 | 幂等设计、重复提交 | W2 | 🔥 完整 |
| 秒杀：Redis 预扣 + **Lua 原子性** | 原子操作、缓存与 DB 一致性 | W3 | 🔥 完整 |
| Kafka 削峰 + 最终一致性 | 异步、补偿、消费幂等、积压处理 | W3 | 🔥 完整 |
| 延迟关单 | 主动讲「Kafka 无延迟队列」的取舍 | W3 | ⚠️ 只做延迟 topic + 轮询 |
| 缓存穿透/击穿基础实现 | 缓存三问 | W3 | ⚠️ 基础实现 + 八股 |
| 缓存雪崩 / Cache Aside 调优 | 缓存一致性 | W3 | ❌ 只背八股 |
| 拆分库存服务：gRPC + etcd | 微服务真实经验、拆分边界 | W4 | 🔥 完整 |
| 超时 / 重试 | 分布式可靠性 | W4 | 🔥 完整 |
| 熔断器（完整实现） | 熔断原理 | W4 | ⚠️ 降级为简单计数器 |
| traceID 透传 | 链路追踪 | W4 | ⚠️ 注入日志，不搭 Jaeger |
| 监控面板 | 可观测性 | W4 | ❌ 只留 `/metrics` 接口 |
| **压测一轮基准数字** | 简历上唯一有说服力的数字 | W4 | 🔥 一轮（QPS/P99/0 超卖） |

### 4.3 Kafka 延迟关单方案（W3 用，二选一）

Kafka **没有原生延迟队列**，这是面试加分点而非缺点，主动讲：

- **方案 A（推荐）**：延迟 topic + 定时轮询兜底。写入 `order.delay`，消费端只处理
  `deadline <= now` 的消息，未到期的重新投递。简单、够用，代价是重复投递开销。
- **方案 B**：内存时间轮（`github.com/RussellLuo/timingwheel`）+ 定时扫描。
  更好讲，但需处理重启后的恢复（可从 DB 重建）。

### 4.4 硬性纪律

- **代码必须上 GitHub**，README 含架构图、启动方式、**设计取舍**。面试官会看。
- **每阶段结束录一次 3 分钟自述**，练到不看稿能讲清「为什么这么设计」。
- **不做玩具 CRUD**。宁可少两个功能，也要把并发和一致性做真实。
- MQ 通用概念（削峰、幂等、死信、补偿、积压）跨 MQ 通用，别只记 API。

---

## 5. 薪资策略（**必须纠正**）

| 项 | 你的想法 | 我的建议 |
| :--- | :--- | :--- |
| 期望 | 18–20K | **前两周不提数字**；W4 项目成型后报 **22–25K** |

**理由**：
1. 上海 5 年经验 Go 岗主流区间 **22–32K**，18–20K 属于明显低报。
2. **报低不提高面试通过率**——筛简历看技术匹配，不看要价。低报的唯一效果是
   面试通过后把自己压死。
3. 被问期望时的话术：**「先看岗位职责和整体包，我更关心技术方向是否匹配。」**
4. 唯一例外：**若现金流已紧迫到必须马上有收入**，18–20K 是合理的风险对冲——
   但你要清楚这是主动放弃的溢价。

---

## 6. 总时间轴

| 阶段 | 日期 | 主线 | 可验证交付物 |
| :--- | :--- | :--- | :--- |
| **W1** | 9/19–9/25 | Go 地基(P0) + 项目骨架 + **简历改稿 + 练手批投递** | 能跑能测的 HTTP 骨架；简历定稿；**9/22 投出练手批** |
| **W2** | 9/26–10/2 | 并发 + 交易核心链路 | 下单/扣库存跑通，**`-race` 无告警**，有并发测试 |
| **W3** | 10/3–10/9 | 数据层 + Kafka + Redis | 秒杀不超卖；延迟关单可用；缓存三问有实现 |
| **W4** | 10/10–10/16 | 微服务拆分 + 可观测性 + **压测** | 两服务 gRPC 调通；Compose 一键部署；压测数据 |
| **W5** | 10/17–10/23 | 简历终稿（**基于真实面试反馈**）+ 八股自测 + 模拟面试 | 简历终稿；高频题能口述；录音自述过关 |
| **W6** | 10/24–10/31 | 面试冲刺 + 复盘迭代 | **offer** |

**关键节奏**：
- **W1 第一动作（两条并行，不是串行）**：
  1. 装 Docker Desktop（1–2h 下载安装，正好用这段时间读切片/map/interface）
  2. **简历 Go 化改稿** —— 投递的瓶颈是简历，不是时间（见 §6.1）
- **保底触发线**：若 **10/20 仍无 Go 面试邀约**，PHP 转为主动投递。

### 6.1 投递节奏（修正版：分三批，9/22 就开投）

投递是一条**转化漏斗**，每一跳都吃时间：

```
投递 → 简历筛选(3–7d) → 一面(+3–7d) → 二面/三面(+5–10d) → HR/offer(+3–7d)
完整闭环：2–4 周（大厂更慢，中小公司更快）
```

| 批次 | 时间 | 投什么 | 目的 |
| :--- | :--- | :--- | :--- |
| **练手批** | **9/22–9/23** | **3–5 家**不心仪的公司（外包等） | **免费市场调研**：简历能否过筛、面试官问什么 |
| **正式第一批** | **9/25–9/30** | 8–10 家，含心仪公司 | 80% 的面试在这里 |
| **第二批 + 补投** | **10/1–10/9**（**含 10/1–10/3 国庆**） | 8–10 家，按反馈修简历 | 覆盖 W6 面试，形成流水线 |
| **补充投递** | **10/12–10/16** | 5–8 家 | 覆盖 W6 后半段面试 |

**累计投递量：24–33 家**，符合 5 年经验岗位的正常转化需求（简历通过率约 20–30%）。

> **国庆假期（10/1–10/3）的取舍**：3 天假期是**连块时间**，用于啃 Redis+Lua 秒杀
> 和 Kafka 延迟关单最高效；但 HR/面试官休假，投递反馈会延迟。
> 建议：**照常投递（不亏），但别把面试安排压在假期**。

**为什么不能等 W3 才投**：首批面试会落到 10/10–10/16，offer 落到 10/20 之后，
W5–W6 只剩两周容错。任何一轮挂掉都没有第二次机会 —— 那不是冲刺，是赌运气。
**要让面试与项目进度并行而非串行：等你坐进面试间时，项目已经做完了。**

### 6.2 渠道优先级

海投转化率远低于内推，**内推需要提前铺垫，不能投递时才想起**。

1. **内推**（最高）—— 从 W1 起每周加 3–5 个目标公司 Go 开发者（脉脉/BOSS），聊技术不聊求职
2. **BOSS 直聊** —— 上海主力渠道，3–7 天回复
3. **猎聘 / 拉勾** —— 5 年经验可挂，主动沟通效率低于 BOSS
4. **公司官网直投** —— 只投明确心仪的目标
5. **V2EX / Go 语言中文网 / GitHub 招聘 issue** —— 补充

**投递台账**（必须建）：记录 `公司 | 岗位 | JD关键要求 | 投递日 | 状态 | 面试问题 | 失分点`。
这是 W5–W6 迭代的唯一依据，也是我做面试复盘时的输入。

---

## 7. 风险与对策

| # | 风险 | 后果 | 对策 |
| :--- | :--- | :--- | :--- |
| R1 | 双轨投递稀释时间 | 两条线都不精 | Go 占 80% 精力；PHP 只做面试复盘；10/20 为触发线 |
| R2 | 微服务被追问框架原理 | 5 年经验答不上更伤 | 用 Gin 手写分层；只吃透自己用到的部分，**不装懂** |
| R3 | Kafka 延迟队列踩坑 | W3 工期超支 | §4.3 已给两个方案，W1 就定选哪个 |
| R4 | **完全放弃算法** | 上海中高级岗多有在线题环节 | **建议降级为「每周 3 题高频」**（数组/字符串/哈希/双指针，欠账最小）；已按你决定暂不做 |
| R5 | 只学不投 | W6 集中面试无迭代空间 | **9/22 投练手批**，10/9 前完成正式第一批（见 §6.1） |
| R9 | 等 W5 才写简历 | 9/22 无简历可投，漏斗无法启动 | **W1 就做简历 Go 化改稿**；W5 只做基于反馈的终稿 |
| R6 | 状态中断（已停学 25 天） | 再次中断则方案失效 | 每日固定「第一个动作」降低启动成本；周末允许减量但不归零 |
| R7 | 环境阻塞 | W2 无数据库，工期连锁崩塌 | **W1 第一动作装 Docker** |
| R8 | 报低薪 | 白丢 3–5K/月 | §5 薪资策略 |

### 7.1 时间预算重算（**基准 6h/天 + 可持续性优先**）

| 节奏 | 工作日 | 有效工时 | 判定 |
| :--- | :--- | :--- | :--- |
| 原假设 43 天 × 8–10h | 43 | 250–300h | ❌ 已废弃 |
| 6 天/周 × 7h | 34 | ~238h | ❌ 你确认 8h 未必稳定 |
| **6 天/周 × 6h** | 34 | **~204h** | ✅ **采用** |
| 5h/天 保底 | 34 | ~170h | ⚠️ 微服务部分须砍掉 |

**结论：总预算 ~204h（34 工作日 × 6h），比最初方案少约 30%。**

> 计算：9/19–10/31 共 43 天，扣除 6 个周日（9/20、9/27、10/4、10/11、10/18、10/25）
> 与 10/31 收尾日 → **34 个工作日**。
> **有余量的日子额外补知识点，但主体计划按 6h 设计 —— 不依赖「状态好」。**

#### 工时分配原则（**项目优先**）

| 时段 | 时长 | 内容 |
| :--- | :--- | :--- |
| 上午 | 2.5h | **项目编码**（不可压缩项） |
| 下午 | 2h | **知识补底**（P0 → P1），学会了立刻写进项目 |
| 傍晚 | 1h | 投递 / 简历 / 口述练习 / 台账更新 |
| 收尾 | 0.5h | 进度表 + 明天的第一个动作 |

> **P0 的正确学法（204h 能撑住的唯一前提）**：**不要先学完再做项目。**
> 每个知识点「学 30 分钟 + 立刻在项目里用上」，远比孤立学 2 小时高效。

#### 6h 预算下的裁剪清单

| 裁掉项 | 省下 | 代价 |
| :--- | :--- | :--- |
| 算法刷题 | ~18h | 部分笔试环节出局（**你已决定，最大风险**） |
| K8s | ~20h | 只到概念层 |
| 微服务完整熔断器 | ~6h | 降级为「超时 + 简单重试」，面试讲取舍 |
| P2 Runtime 深挖（源码级） | ~12h | 只到「能讲结论和影响因素」 |
| 链路追踪完整方案（Jaeger 全套） | ~8h | 降级为 **traceID 注入日志**，讲清原理 |
| 监控（Prometheus + Grafana） | ~10h | 降级为结构化日志 + `/metrics` 接口 |
| 压测多轮调优 | ~5h | **保留一轮基准数字**（简历刚需） |
| Cache Aside 深度调优 | ~5h | 只做穿透/击穿**基础实现** + 八股背诵 |
| 延迟关单（仅留方案 A） | ~4h | 放弃时间轮方案 |

**保住不砍**（面试价值最高）：订单事务与库存扣减、Redis+Lua 秒杀防超卖、
Kafka 削峰与消费幂等、gRPC + etcd 真实拆分、**一轮压测数字**。

#### 可持续性设计（**防止第二次中断**）

你 8/24 断掉过一次，连续 25 天。**方案失效的头号风险不是学不会，是撑不住。**

| 机制 | 具体做法 | 作用 |
| :--- | :--- | :--- |
| **周日完全休息** | 不碰代码、不看八股 | 消除"欠债感" |
| 6h 基准、8h 天花板 | 状态好可多做，但**不设必须完成的日额** | 防止过载 |
| **每周留半天缓冲** | 吸收工期延后，而不是推进新内容 | 防止雪崩 |
| 固定"第一个动作" | 每天开机 5 分钟内做一件明确的小任务 | 降低启动成本 |
| **允许减量但不归零** | 状态差的日子做 2h 也算达标 | 保住惯性 |
| 每周日复盘 15 分钟 | 填 §8 进度表 | 早发现跑偏 |

**如果连续 3 天做不满 3h，立刻告诉我** —— 那是方案需要缩减的信号，不是你需要硬扛的信号。

---

## 8. 每日执行模板（6h 基准版）

| 时段 | 时长 | 内容 |
| :--- | :--- | :--- |
| 上午 | 2.5h | **项目编码** —— 当前阶段主线任务，先动手再看文档 |
| 下午 | 2h | **知识补底**（P0 → P1）+ 立刻在项目里用上 |
| 傍晚 | 1h | 面试题**口述**练习 / 投递跟进 / 台账更新 |
| 收尾 | 0.5h | 填下方进度表一行；写下明天的「第一个动作」 |
| **合计** | **6h** | 状态好可到 8h，但**不设必须完成的日额** |

**周日：完全休息。** 不碰代码、不看八股。这是维持 6 周的必要成本，不是偷懒。

- **收尾一行**：`日期 | 今天完成了什么 | 卡在哪 | 明天的第一个动作`

### 实际进度

- **2026-09-18（周五）**：方案定稿（v1）+ **环境搭建完成大部分**
  - ✅ C 盘清理 25GB → 42.8GB（关休眠 -12.58GB、剪映缓存 -4.75GB）
  - ✅ WSL2 安装完成，Ubuntu 装在 `D:\DevSoft\WSL\Ubuntu`（1.42GB），账号已设
  - ✅ 开发环境决策：主力 WSL，代码迁至 `~/go-learning`
  - ⬜ 待办：WSL 内装 Go、Docker Desktop 装到 `D:\DevSoft\Docker`、Docker 数据迁 D 盘
- **关键路径提示**：9/18 全天用于环境（原计划 9/19 才做），
  因此 W1 的 P0 学习实际可用天数减少。**简历改稿不可再推迟。**

---

## 9. 保障机制：我做你的陪练

你已确认需要「定时出方案 + 当面试官做模拟面试/复盘」。节奏如下：

| 时点 | 我做什么 | 你需要提供什么 |
| :--- | :--- | :--- |
| 每日或每 2–3 天 | 检查进度、纠偏、调整次日任务 | §8 的收尾一行，或直接说进展 |
| **W1 前半（9/19–9/21）** | **审查简历 Go 化改稿**（定位、项目表述、能否过 Go 岗筛选） | 简历初稿（文字即可） |
| **W1 末（9/25）** | 审你的项目骨架（分层/错误处理/并发安全） | 代码目录 或 GitHub 链接 |
| **W2 末（10/2）** | 第一轮模拟面试：**Go 基础 + 并发 + 你的项目** | 10 分钟自述录音/文字 |
| **W3 末（10/9）** | 第二轮模拟：**秒杀/一致性/缓存/Kafka 追问** | 同上 |
| **W4 末（10/16）** | 第三轮模拟：**微服务/分布式/架构取舍** + 简历终稿诊断 | 简历当前版 + 面试反馈 |
| **W5 末（10/23）** | 全真模拟（含压力追问、薪资谈判演练） | —— |
| W6 | 每场真实面试后当天复盘，定位失分点并定向补 | 面试问题清单（尽量回忆原话） |

**当前待完成（两件，可并行）**：
1. Docker Desktop 装到 `D:\DevSoft\Docker`，`docker run --rm hello-world` 跑通
2. 把现有简历（脱敏后）发我做 **Go 岗视角改稿审查** —— 因为 9/22 要投练手批
   （环境已不再是借口：WSL 已就绪，Docker 可挂着下载）

---

## 10. 待决项

- ~~简历双版本~~ → **已决：单份 Go 优先简历**，工作经历如实写 PHP，用「可迁移方法论」解释背景。
  执行细节见 `W1-打卡表与简历自检.md`。
