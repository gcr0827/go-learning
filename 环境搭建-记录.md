# 环境搭建记录（Docker Desktop + WSL2）

> 配套 `PLAN-2026-10-Go求职冲刺.md` 的第 1 项 W1 任务。
> 目标：`docker run --rm hello-world` 成功 + Compose 能起 MySQL/Redis/Kafka。

---

## 0. 本机探测结果（2026-09-18）

| 项 | 实测值 | 结论 |
| :--- | :--- | :--- |
| OS | build **26200**.9168，DisplayVersion **25H2** | 实为 **Windows 11 25H2**（注册表 ProductName 写 Windows 10 Home 是已知显示问题） |
| Go | **1.26.5** windows/amd64 | ✅ 可用 |
| Git | 2.51.0.windows.1 | ✅ 可用 |
| GOPROXY | `https://goproxy.cn,direct` | ✅ 国内拉包正常 |
| `wsl.exe` | 存在，但 `wsl --status` 报「未安装用于 Linux 的 Windows 子系统」 | ⚠️ 需执行 `wsl --install` |
| Docker | **未安装** | ⚠️ 待安装 |
| 端口 3306/6379/5672/9092/2379 | **全部无监听** | ⚠️ 无 MySQL/Redis/MQ/etcd |
| 磁盘 / 内存 / 虚拟化 | 见 §0.1 | |

### 0.1 磁盘清理结果（2026-09-18 完成）

原始状态：**C 盘可用仅 25 GB**（低于 Docker 舒适线），实测占用如下：

| 占用项 | 原始 | 处理 | 释放 |
| :--- | :--- | :--- | :--- |
| `hiberfil.sys` 休眠文件 | 12.58 GB | `powercfg /h off` | **12.58 GB** |
| `AppData\Local\JianyingPro` | 6.64 GB | 删 `User Data\Cache`（纯缓存） | **4.75 GB** |
| 其他（Temp/更新残留等） | — | 清理 | ≈9.5 GB |

**清理后**：

| 盘 | 总量 | 可用 |
| :--- | :--- | :--- |
| **C:** | 200 GB | **51.8 GB** ✅ |
| **D:** | 731.7 GB | **679.3 GB** 🎉 |

**战略结论**：D 盘有 679GB 空闲，因此——
- **Docker 的 Disk image location 必须改为 `D:\DockerData`**（而非默认 C 盘）
- 项目代码保持 `D:\Program\go-learning`
- C 盘的 51.8GB 只承担系统与 IDE，足够宽裕

**已知副作用**：`go` 命令会尝试写 `%APPDATA%\go\telemetry` 失败并打印报错。
已建议 `go env -w GOTELEMETRY=off`（或 `go telemetry off`）消除噪音。

---

## 1. 开工前必须确认的两项

| 检查项 | 要求 | 状态 |
| :--- | :--- | :--- |
| **C 盘剩余空间** | ≥ 30GB | ✅ **51.8 GB**（已清理，见 §0.1） |
| **D 盘剩余空间** | —— | ✅ **679.3 GB** → Docker 数据放这里 |
| **内存** | **≥ 8GB** | ✅ **16GB+** |
| **虚拟化** | 必须「**已启用**」 | ✅ **已启用**（任务管理器确认） |

**结论**：所有硬件前提通过，Kafka 可直接使用，无需降级到 Redpanda。

- 虚拟化显示「已禁用」→ 重启进 BIOS 开 **SVM Mode**（AMD）/ **Intel VT-x**（Intel）。这是唯一无法在系统内完成的一步。
- ~~内存只有 8GB → 备选 Redpanda~~ **不适用：已确认 16GB+，Kafka 直接用。**

### 1.1 安装位置方案（**已定：D 盘为主**）

WSL 分三块，位置规则不同：

| 组件 | 位置 | 能否到 D 盘 | 大小 |
| :--- | :--- | :--- | :--- |
| WSL 系统组件（`wsl.exe`、内核、虚拟机平台） | **C 盘（强制）** | ❌ Windows 组件 | ~0.5 GB |
| **Ubuntu 发行版**（ext4.vhdx） | 默认 C 盘 | ✅ **迁到 `D:\WSL\Ubuntu`** | ~3 GB |
| **Docker 数据**（`docker-desktop-data`） | 默认 C 盘 | ✅ **迁到 `D:\WSL\DockerData`** | ~5–8 GB |
| Docker Desktop 程序本体 | 默认 `C:\Program Files\Docker` | ✅ `--installation-dir=D:\DevSoft\Docker` | ~4 GB |

→ **最终 C 盘只增加 ~0.5 GB，D 盘用掉 ~12–15 GB（余 664 GB）。**

#### 为什么不用 `wsl --install --location`

`--location` 需要 Store 版 WSL 2.4.4+，本机 `wsl.exe` 为系统自带 26100.8737，可能不支持。
**改用官方 export/import 路线，100% 有效且可复用。**

#### 迁移步骤（Ubuntu）

```powershell
wsl --shutdown
wsl --export Ubuntu D:\WSL\ubuntu.tar
wsl --unregister Ubuntu
wsl --import Ubuntu D:\WSL\Ubuntu D:\WSL\ubuntu.tar --version 2
wsl -l -v
```

**易漏步骤**：`--import` 后默认用户变回 `root`，需恢复：

```powershell
wsl -d Ubuntu -u root        # 进入后：adduser <name>; usermod -aG sudo <name>; exit
ubuntu config --default-user <name>
# 若 ubuntu.exe 不存在，改用注册表：
# Get-Item "HKCU:\Software\Microsoft\Windows\CurrentVersion\Lxss\*" |
#   Where-Object { $_.GetValue("DistributionName") -eq "Ubuntu" } |
#   Set-ItemProperty -Name DefaultUid -Value 1000
```

确认无误后删 tar 省空间：`Remove-Item D:\WSL\ubuntu.tar`

#### Docker 数据迁移

⚠️ **不要用 Docker Desktop 的「Disk image location」按钮** —— 已知 bug：
[docker/for-win#14904](https://github.com/docker/for-win/issues/14904)、
[#14142](https://github.com/docker/for-win/issues/14142)（`WSL_E_DISTRO_NOT_FOUND` 等迁移失败）。

✅ 用 WSL 手法迁 `docker-desktop-data`：

```powershell
wsl --shutdown
wsl --export docker-desktop-data D:\WSL\docker-data.tar
wsl --unregister docker-desktop-data
wsl --import docker-desktop-data D:\WSL\DockerData D:\WSL\docker-data.tar --version 2
```

> 新版本 Docker Desktop 安装时会询问数据位置，若询问则**直接选 D 盘**，可省掉此步。
> **必须在拉任何镜像之前完成。**

#### 限制 WSL2 资源占用

新建 `C:\Users\zheng\.wslconfig`（16GB 内存，Kafka 很吃内存）：

```ini
[wsl2]
memory=6GB
processors=6
swap=4GB
```

#### WSL 内的 Go 环境（独立于 Windows）

**重要认知**：Windows 的 Go 与 WSL 的 Go 是**两套独立环境**，互不覆盖、互不失效。
Windows Go 仍可用于：小 demo 验证、交叉编译 `GOOS=linux`、WSL 故障时备用。
**但主力开发环境已选定 WSL**（理由：Linux = 生产环境，`-race`/压测更准，Docker 原生）。

```bash
# ① 装 Go（用国内镜像站 golang.google.cn）
wget https://golang.google.cn/dl/go1.26.5.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.26.5.linux-amd64.tar.gz

cat >> ~/.bashrc <<'EOF'
export PATH=$PATH:/usr/local/go/bin:$HOME/go/bin
export GOPROXY=https://goproxy.cn,direct
export GOTOOLCHAIN=local
EOF
source ~/.bashrc
go version && go env GOPROXY
```

`GOTOOLCHAIN=local` **必加**：Go 1.21+ 默认自动下载工具链，国内网络会卡死。

#### 代码位置（**已定：WSL 文件系统内**）

| 方案 | 编译速度 | 结论 |
| :--- | :--- | :--- |
| `/mnt/d/Program/go-learning` | 慢 5–10 倍（跨文件系统 IO） | ❌ 否决 |
| **`~/go-learning`**（WSL 内） | **快** | ✅ **已选定** |

迁移：`cp -r /mnt/d/Program/go-learning ~/go-learning`（先复制，验证无误再清理 Windows 侧）

Windows 侧访问：`\\wsl$\Ubuntu\home\<用户名>\go-learning`

#### GoLand 配置

1. File → Open → `\\wsl$\Ubuntu\home\<用户名>\go-learning`
2. Settings → Go → GOROOT → `\\wsl$\Ubuntu\usr\local\go`
3. Settings → Go → GOPATH → WSL 内 `~/go`
4. 用 GoLand 内置 Terminal（默认起 WSL 终端）验证 `go build`

#### ⚠️ 代码丢失风险（必须处理）

WSL 文件系统在 `ext4.vhdx` 内，**不在 Windows 常规备份覆盖范围**，
且 `wsl --unregister` 会瞬间清空。项目代码是简历核心资产，必须：

1. **推到 GitHub**（同时作为简历加分项）
   ```bash
   ssh-keygen -t ed25519 -C "your-email"
   cat ~/.ssh/id_ed25519.pub   # 粘到 GitHub → Settings → SSH keys
   ssh -T git@github.com
   ```
2. **定期导出备份**
   ```powershell
   wsl --export Ubuntu D:\backup\ubuntu-$(Get-Date -Format yyyyMMdd).tar
   ```

#### WSL 磁盘只涨不缩（已知问题）

删除文件后 `ext4.vhdx` 不会自动缩小，空间紧张时压缩：

```powershell
wsl --shutdown
Optimize-VHD -Path "D:\DevSoft\WSL\Ubuntu\ext4.vhdx" -Mode Full   # 需 Hyper-V 模块
```

---

## 2. 安装步骤

### 第 1 步：启用 WSL2（**管理员** PowerShell）

```powershell
wsl --install
```

然后 **重启电脑**。重启后 Ubuntu 会要求设置 Linux 用户名与密码（记下来）。

验证：

```powershell
wsl --status
wsl -l -v          # 应显示 Ubuntu，VERSION = 2
```

### 第 2 步：安装 Docker Desktop

```powershell
winget install --id Docker.DockerDesktop -e
```

或手动下载安装包（**勾选 "Use WSL 2 instead of Hyper-V"**）：
https://www.docker.com/products/docker-desktop/

> 国内下载慢（常见 100KB/s）→ 找国内镜像站的离线安装包，或挂代理。
> **等待时间用来读切片/map/interface，不要刷手机。**
> 安装后需要重启，首次启动要接受服务条款。

### 第 3 步：配置镜像加速（**不做这步，拉 MySQL/Kafka 会超时**）

Docker Desktop → **Settings → Docker Engine**，改为：

```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://dockerproxy.com",
    "https://mirror.ccs.tencentyun.com"
  ]
}
```

点 **Apply & Restart**。

### 第 4 步：验证

```powershell
docker run --rm hello-world
docker version
docker compose version
```

---

## 3. 国内网络特别提醒（你的场景必然遇到）

1. **拉 `mysql` / `redis` 官方镜像可能失败** → 第 3 步的镜像加速必须配。
2. **拉 `kafka` 镜像更慢**（几百 MB）→ 若反复失败，改用 Redpanda：
   `docker pull redpandadata/redpanda`，API 兼容 Kafka。
3. **Go 依赖不受影响** —— 你已设 `GOPROXY=goproxy.cn`，✅。

---

## 4. WSL 与代码位置的取舍（**踩坑高发区**）

| 方案 | 优点 | 缺点 | 建议 |
| :--- | :--- | :--- | :--- |
| 代码放 Windows（`D:\Program\go-learning`）+ WSL 里挂载访问 | Windows 侧 IDE 顺手、Git 方便 | WSL 访问 `/mnt/d/...` **IO 慢 5–10 倍**，编译明显变慢 | 项目初期够用 |
| 代码放 WSL 内部（`~/go-learning`） | 编译快、容器挂载快 | Windows IDE 访问要走 `\\wsl$\` | 等编译慢到影响效率时再迁 |

**决定**：**先保持 `D:\Program\go-learning` 不动**（你已经在这里学习，迁移成本不值当）。
如果 W4 拆分微服务后 `go build` 慢到影响节奏，再迁到 `~/go-learning`。
迁移时注意：`go env -w GOMODCACHE` 在 WSL 里要重新配。

> **重要**：Docker 容器里的服务（MySQL/Redis/Kafka）从 Windows 宿主访问用
> `localhost:3306` 等端口即可（Docker Desktop 自动做端口转发）。
> 但 Go 程序**在 Windows 上跑**时连 `localhost` 没问题；
> 若将来把 Go 程序也放进容器，则要改用 Docker 网络里的服务名。

---

## 5. 常见报错速查

| 现象 | 原因 | 解法 |
| :--- | :--- | :--- |
| `wsl --install` 报「无法解析服务器的名称」 | 需管理员权限 / DNS 问题 | 管理员 PowerShell 重试；或开 VPN |
| WSL 装完仍报「没有已安装的发行版」 | 发行版未装 | `wsl --install -d Ubuntu` |
| Docker Desktop 启动报 `WSL 2 is not installed` | WSL2 未完成或未重启 | 确认 `wsl -l -v` 显示 VERSION 2，未重启则重启 |
| Docker 启动报 `Virtualization support not detected` | BIOS 虚拟化未开 | 进 BIOS 开 SVM / VT-x |
| Docker Desktop 卡在 "Starting..." 很久 | 首次初始化慢 / 内存不足 | 等 5–10 分钟；仍不行看内存是否 <8GB |
| `docker run` 拉镜像卡住或 403 | 镜像源问题 | 检查 §2 第 3 步；换镜像源 |
| `docker` 命令找不到 | PATH 未生效 | 重启终端；或重启电脑 |
| 容器起不来，报端口被占用 | 3306/6379 被本机其他程序占用 | `netstat -ano \| findstr 3306` 找进程 |

---

## 6. 验收标准（W1 必须达成）

- [ ] `wsl -l -v` 显示 Ubuntu，VERSION = **2**
- [ ] `docker version` 正常输出 Client 与 Server
- [ ] `docker run --rm hello-world` 成功
- [ ] `docker compose version` 可用
- [ ] （W1 末）`docker compose up -d` 能起 **MySQL + Redis**，3306/6379 有监听
- [ ] （W3）能起 **Kafka** 或 Redpanda，9092 有监听

---

## 7. 实测记录

| 日期 | 步骤 | 结果 / 报错 |
| :--- | :--- | :--- |
| 09-18 | 环境探测 | Windows 11 25H2 (26200)，WSL 未装，Docker 未装 |
