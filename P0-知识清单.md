# P0 知识清单（Go 面试高频核心）

> 配套 `PLAN-2026-10-Go求职冲刺.md` §3。
> **纪律：每个知识点 = 可运行 demo + 3 句自己的结论 + 对着录音讲 90 秒。**
> 验收标准不是「看完了」，是**能不看稿讲出来**。

---

## 总览与排期

| # | 主题 | 面试频率 | 投入 | 排期 |
| :--- | :--- | :--- | :--- | :--- |
| ① | 切片 slice | 极高 | 2.5h | W1（9/19） |
| ② | map | 极高 | 2h | W1（9/21） |
| ③ | interface | 极高 | 3h | W1（9/22） |
| ④ | error 处理 | 高（**工程能力试金石**） | 3h | W1（9/23） |
| ⑤ | defer | 中高 | 1.5h | W1（9/24） |
| ⑥ | 并发原语 | **最高** | 6h | W1（9/24）+ W2 前半 |
| ⑦ | context | 高 | 3h | W2 前半 |

**合计约 21h**，占 W1–W2 下午时段（2h/天 × 10 天 = 20h）。
⑥⑦ 部分落在 W2 —— **在订单项目里用到时学，比孤立学更牢**。

---

## ① 切片 slice（2.5h）

### 必须能答
- [ ] `len` 与 `cap` 的区别
- [ ] `append` 扩容策略（**1.18+ 已改**：<256 翻倍，≥256 约 1.25 倍增长）
- [ ] 切片传参是值传递还是引用传递？（**切片头是值传递，底层数组共享**）
- [ ] 为什么 `append` 后原切片有时变、有时不变？（取决于是否触发扩容）

### 必写 demo（3 个）
- [ ] 共享底层数组的坑：`a := s[1:3]; a[0] = 99` → `s[1]` 也变
- [ ] `append` 何时触发新数组（cap 够 vs 不够，打印地址对比）
- [ ] 传参陷阱：函数内 `append` 会不会影响外部切片？

### 项目应用
批量插入订单时切分批次 —— 注意别共享底层数组。

### 90 秒自述
- [ ] 录音通过

---

## ② map（2h）

### 必须能答
- [ ] **为什么 map 并发写会 panic？**（运行时检测到并发写直接 `fatal error`，**不可 recover**）
- [ ] `sync.Map` 什么时候用？（读多写少、key 相对固定；**不是**万能替代 `Mutex+map`）
- [ ] 遍历顺序为什么随机？（故意设计，防止依赖顺序）
- [ ] 扩容机制（渐进式 rehash，每次操作搬迁一部分）

### 必写 demo（2 个）
- [ ] 复现并发写 panic（两个 goroutine 同时写）
- [ ] 对比 `Mutex+map` 与 `sync.Map` 的写法差异

### 项目应用
库存扣减的本地缓存、订单状态机映射。

### 90 秒自述
- [ ] 录音通过

---

## ③ interface（3h）

### 必须能答
- [ ] 鸭子类型：Go 接口是**隐式实现**
- [ ] **经典坑**：`var p *T = nil; var i interface{} = p; i != nil` 为何是 `true`？
      （接口值 = 类型指针 + 数据指针，类型指针非空 → 接口非空）
- [ ] 类型断言 `v, ok := i.(T)` 与 `type switch`
- [ ] 空接口 `interface{}` / `any` 的开销

### 必写 demo（3 个）
- [ ] 复现 `(*T)(nil)` 装进 interface 后 `!= nil` 的坑
- [ ] 隐式实现：自定义类型"自动"满足接口
- [ ] type switch 分发不同订单类型

### 项目应用
**repository 抽成接口，service 依赖接口而非实现** —— 这是分层的核心。

### 90 秒自述
- [ ] 录音通过

---

## ④ error 处理（3h）

### 必须能答
- [ ] `errors.Is` vs `errors.As` 的区别与用法
- [ ] `%w` 包装错误链、`errors.Unwrap`
- [ ] **错误分层**：业务错误（可返回用户）vs 系统错误（记日志、不暴露）
- [ ] `panic` / `recover` 的正确边界（**不要用 panic 做流程控制**）
- [ ] 为什么 Go 不用异常？（显式错误是设计选择）

### 必写 demo（3 个）
- [ ] 定义业务错误类型 + 用 `errors.As` 提取
- [ ] `%w` 包装链 + `errors.Is` 判断根源
- [ ] HTTP 层统一把错误映射成响应码

### 项目应用
repository 返回 `(data, error)`，**不返回 message 字符串**
—— 这正是你 Laravel 规范里「Model 返回纯数据」的 Go 版。

### 90 秒自述
- [ ] 录音通过

---

## ⑤ defer（1.5h）

### 必须能答
- [ ] 执行顺序（**LIFO**，后进先出）
- [ ] **与 return 的交互**：return 先给返回值赋值 → 执行 defer → 真正返回
- [ ] 命名返回值 + defer 修改返回值的陷阱
- [ ] 循环里 defer 的性能问题（1.14+ 已优化，但循环里仍要小心）

### 必写 demo（2 个）
- [ ] defer + 命名返回值：理解为何能改返回值
- [ ] 循环里 defer 文件句柄不释放的问题

### 项目应用
事务回滚、锁释放、连接归还。

### 90 秒自述
- [ ] 录音通过

---

## ⑥ 并发原语（6h）**投入最大、面试最重**

### 必须能答
- [ ] goroutine 生命周期；为什么 Go 没有强制终止 goroutine 的机制
- [ ] channel：无缓冲 vs 有缓冲的阻塞语义
- [ ] **channel 三条规则**：谁负责关闭 / 关闭后读什么 / 重复关闭会 panic
- [ ] `select` + `ctx.Done()` 的协作式取消
- [ ] `sync.Mutex` / `RWMutex` / `Once` / `WaitGroup` 的适用场景
- [ ] `errgroup` 怎么用（并发任务 + 错误收集）

### 必写 demo（5 个）
- [ ] 无缓冲 / 有缓冲 channel 的阻塞差异
- [ ] channel 关闭的三条规则演示
- [ ] `select` 多路复用 + 超时退出
- [ ] `WaitGroup` 并发任务 + 错误收集（对比 `errgroup`）
- [ ] `Mutex` 保护共享计数器 vs 用 channel 传递

### 项目应用
订单并发扣库存、秒杀请求聚合、Kafka 消费者并发处理。

### 90 秒自述
- [ ] 录音通过

---

## ⑦ context（3h）

### 必须能答
- [ ] context 是**树形结构**，取消会级联传播
- [ ] `WithCancel` / `WithTimeout` / `WithDeadline` 的区别（Timeout 底层就是 Deadline）
- [ ] **为什么是「协作式取消」**：关闭 Done channel 只是发信号，
      调用方不检查就不会被中断（Go 没有抢占式终止 goroutine 的机制）
- [ ] context 应作为**第一个参数**传递，不要存进 struct

### 必写 demo（3 个）
- [ ] 手动 cancel + 多个 goroutine 监听 Done
- [ ] `WithTimeout` 超时后 `ctx.Err()` 返回什么
- [ ] 级联取消：父 cancel 后子也收到信号

### 项目应用
HTTP 请求超时传递到 DB 查询和 RPC 调用 —— **这是 W4 微服务的基础**。

### 90 秒自述
- [ ] 录音通过

---

## 三条执行纪律

1. **每个知识点写「可运行 demo + 3 句自己的结论」，禁止摘抄教程**
   （现有 `notes/` 是摘抄式的，所以停了 25 天也没沉淀下东西）

2. **学完立刻用进项目**（204h 能撑住的唯一方式）
   - 学 ④ error → 当天改 repository 的错误处理
   - 学 ③ interface → 当天把 repository 抽成接口
   - 学 ⑦ context → 当天给 HTTP handler 加超时传递

3. **验收 = 能对空气讲 90 秒**
   每项学完对着录音讲一遍，讲不顺就是没学透

---

## 学习记录

| 项 | 开始 | 完成 demo | 90秒自述 | 已用于项目 |
| :--- | :--- | :--- | :--- | :--- |
| ① 切片 | 09-18 | ✅ | ⬜ | ⬜ |
| ② map | | ⬜ | ⬜ | ⬜ |
| ③ interface | | ⬜ | ⬜ | ⬜ |
| ④ error | | ⬜ | ⬜ | ⬜ |
| ⑤ defer | | ⬜ | ⬜ | ⬜ |
| ⑥ 并发 | | ⬜ | ⬜ | ⬜ |
| ⑦ context | | ⬜ | ⬜ | ⬜ |

> **① 切片已完成（09-18）**：3 个 demo + 结论 + 源码级验证（`nextslicecap` / `roundupsize`）。
> 产出：`切片/切片Q&A-面试八股.md`、验证程序 `切片/slice-verify/main.go`。
> **待补**：90 秒自述录音（9/19 傍晚）。
