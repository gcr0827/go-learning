## 实验1：扩容策略

1. cap变化规律：以2的次方增长
2. 小容量 vs 大容量的阶段差异：小容量时切片会产生频繁扩容的情况
3. 为什么这么设计：不太理解，另外对于原始切片的cap大小也存在疑问



## 实验2：共享底层数组

1. 我预测 a =[1,2,3,4,5,99] ，实际 a = [1 2 3 99 5]，差异原因：切片a没有发生扩容，所以认为append应该是最后一个值追加
2. cap(b) 为什么是4：不太理解
3. a[1:3:3] 的作用：起始下标:结束下标:容量上限，但不理解为什么这种情况下不影响切片a



## 实验3：传参拷贝语义

1. 为什么 s[0] 修改生效、append 不生效：我理解modify方法中append应该也会生效啊，切片内部有指针，也没发生扩容，我理解是要生效的
2. 切片是值传递还是引用传递（标准答法）：go中所有的参数都是值传递，只不过切片结构内部含有指针
3. 改成 *[]int 后行为变化：入参变成传指针