# 简历事实核对与改写方案

> 配套 `简历改写清单.md`、`W1-执行方案.md` 9/19。
> **核对日期**：2026-09-18/19
> **结论**：原模板中 7 处量化数字**无真实依据（编造）**，gRPC **未真实使用** → 全部按「删除数字 + 保留真实事实」改写。

---

## 1. 核心决策（已确认）

| 事项 | 决定 | 理由 |
| :--- | :--- | :--- |
| 主叙事 | **Go 主叙事**（18 个月真实 Go 生产经验） | 万级设备 + Goroutine/Channel 并发是真货 |
| 编造的 7 个数字 | **全部删除** | 编的数字撑不过追问，且会污染真实内容 |
| gRPC | **从旧项目删除**，改用 6 周个人项目支撑 | 「我项目里用过」比「工作里用过但答不上」强 |
| Gin | 从监控项目技术栈删除 | 未真实使用 |
| Go 经验时长 | 不写「X 年 Go」，面试口头说「18 个月 + 持续在做项目」 | 诚实且更有说服力 |

### 为什么"学完挂到旧项目"不可行

**因为面试官拉的不是"钩子"，是"网"。**

```
简历写「项目采用 gRPC」→ 面试官追问
  ├─ proto 怎么分包的？
  ├─ 拦截器写了什么？超时怎么传递？
  ├─ 为什么选 gRPC 不选 HTTP？迁移踩了什么坑？
  ├─ 上线时怎么做灰度？出过什么问题？
  └─ 这些"决策与过程"你补不了 —— 它们必须发生过
```

**能补的**：技术原理（gRPC 是什么、Protobuf 为什么快）
**补不了的**：你在那个项目里的决策、踩坑、协作、上线过程

---

## 2. 逐条核对结果

### A. 删除（编造，无依据）

| # | 原位置 | 原文 | 处理 |
| :--- | :--- | :--- | :--- |
| 1 | 行 161 | 接口平均响应时间从 **320ms 降至 90ms** | ❌ 删除 |
| 2 | 行 162 | 搭建 **Kubernetes + GitLab CI** 流水线，发布效率提升 **60%** | ❌ **整条删除**（未搭过） |
| 3 | 行 163 | 支撑**日均千万级请求** | ❌ 删除或换成真实业务量 |
| 4 | 行 184 | 系统整体性能提升 **40%** | ❌ 删除数字，保留动作 |
| 5 | 行 185 | 万级设备拉取**毫秒级** | ❌ 表述矛盾（1 万台不可能毫秒级） |
| 6 | 行 198 | 系统响应速度提升 **25%** | ❌ 删除数字 |
| 7 | 行 209 | 响应速度 **30%** | ❌ 删除数字，**保留 Redis+Kafka 事实** |

### B. 保留（真实）

| 位置 | 内容 | 说明 |
| :--- | :--- | :--- |
| 行 227 | 使用 Go 优化设备数据采集逻辑 | ✅ 真实 |
| 行 244 | Goroutine + Channel 重构 PHP 采集流水线 | ✅ **真实且最值钱** |
| 行 243 | Redis String/Hash 缓存热点数据 | ✅ 保留 |
| 行 255–260 | 腾讯企点：适配器模式 / 分表 / Redis 分布式锁 / ZSET / ES | ✅ 真实技术点，质量高 |
| 行 196 | 黑焰 WMS 需求分析与评审、主导模块迭代、设计核心包 | ✅ 保留（「带领 5 人」待确认是带人还是团队内） |

### C. 待确认（需你回忆）

| # | 位置 | 待确认 |
| :--- | :--- | :--- |
| 8 | 行 199 | EPOS 项目 3 个 `todo` 要填什么 |
| 9 | 行 196 | 「带领团队（5人）」= 真带人，还是「在 5 人团队中」 |
| 10 | 行 173–174 | 独立开发（珂尔玛煤矿平台）具体做了什么 |
| 11 | 行 280 | 「**7 年**经验」→ 实际 6.5 年（2019.03 起） |
| 12 | 监控项目 | 采集**并发度**设置（开多少 goroutine） |

---

## 3. 改写后的关键段落

### 3.1 数字化运维监控系统（**放项目经验第一位**）

```
项目名称：数字化运维监控系统          角色：后端（Go 采集服务）   2023.07 — 2024.12
技术栈：Go · Goroutine/Channel · Redis · MySQL · Yaf

- 负责数字化运维监控系统的数据采集服务，覆盖万级设备的分钟级采集。
- 将原 PHP 串行采集重构为 Go 并发采集：用 Goroutine + Channel 控制并发度，
  配合超时与失败重试机制，避免单轮采集堆积影响下一轮调度。
- 用 Redis String/Hash 缓存设备热点状态，降低采集端与数据库压力。
- 参与设备数据采集链路的方案设计，推动 PHP 侧业务与 Go 采集服务的边界划分。
```

**为什么这样写更强**：
- 第 2 条能让面试官问 20 分钟（并发度/背压/超时/重试/正确性验证）
- 没有百分比，但「万级 + 分钟级 + 并发重构」已经足够
- 面试官可自行推算「约 167 次采集/秒」——**推导出来的数字比你给的更可信**

### 3.2 新米汇（2025.08–至今）

```
- 负责 EPOS 点餐系统订单、菜品、门店核心模块的后端开发与迭代。
- ___（一条真实做过的优化或改进，无数字则写动作与机制）
```

**⚠️ 必须删除**：`主导 XXX 服务重构`、`K8s + GitLab CI 流水线`、`带领 X 人`、`日均千万级请求`。

### 3.3 腾讯企点（2019.03–2021.03）— 只删数字

```
- 系统采用 Yii2 作为服务框架，对外暴露接口与其他服务通信交互。
- 采用适配器模式对接不同服务商，提升代码复用率。
- 针对大量数据实施分表存储方案，降低 MySQL 负载、提升查询效率。
- 利用 Redis 分布式锁，避免并发场景下的数据不一致。
- 使用 Redis ZSET 和 LIST 实现热词记录与搜索历史功能。
- 引入 Elasticsearch 增强全文检索能力，实现关键词高亮与数据分析。
```

（**只删掉「响应速度 30%」**，其余都是真实技术点）

### 3.4 个人优势（用事实替代形容词）

```
技术实践  6.5 年后端开发经验。其中 18 个月负责 Go 数据采集服务，
          用 Goroutine + Channel 重构采集流水线，覆盖万级设备的分钟级采集。
          正在以 Go 独立开发电商交易链路项目（订单/库存一致性、秒杀削峰、gRPC 服务拆分）。
业务背景  参与 WMS 仓储系统、数字化运维监控、腾讯企点等系统，
          熟悉订单、库存、设备数据等业务链路与一致性设计。
工程能力  推行代码审查与开发规范；熟悉 Docker、CI/CD、单元测试与性能优化。
```

**删除**：「7 年」「深耕 Go 高并发与分布式系统架构设计」「学习能力」条。

---

## 4. gRPC 的正确处理路径

### 4.0 ⚠️ 先确认一个事实（9/19 第一件事）

**你的 Go 采集服务，数据是怎么上报给后端的？**

```
采集 Agent（你写的 Go 并发采集）
      ↓ ← 这一步是什么？gRPC / HTTP / 写 DB / 写 MQ / 记不清
后端服务
```

| 答案 | 简历怎么写 | 面试能答什么 |
| :--- | :--- | :--- |
| **gRPC（我是调用方）** | 「采集服务通过 gRPC 上报数据到后端」 | 调用模式、超时传递、重试、连接复用 → **不用编任何东西** |
| HTTP / 写 DB / 写 MQ | 如实写那种方式 | 不声称用过 gRPC |
| 记不清 | **先不写 gRPC**，等个人项目做完再补 | 保守但零风险 |

**怎么确认**：翻旧 Git 仓库、看 `go.mod` 依赖里有没有 `google.golang.org/grpc`、或问前同事。

> **判断标准**：如果你是 gRPC 的**客户端调用方**（调用别人写好的 stub），
> 那你就是 gRPC 的**真实使用者** —— 如实写即可，不需要编造。

### 4.1 补 gRPC 的计划（无论上面答案如何都要做）

| 时间 | 动作 |
| :--- | :--- |
| **W1–W2** | 学 gRPC 基础（proto 定义、四种调用模式、拦截器、超时传递） |
| **W3–W4** | 在个人项目里**真实实现**：库存服务拆成独立 gRPC 服务 + etcd 服务发现 |
| **简历** | 写在**个人项目**下，标注「进行中」，带 GitHub 链接 |
| **面试话术** | 见下方（按 §4.0 的答案二选一） |

**话术 A（若采集确实用了 gRPC）**：
> 「采集服务是我用 Go 写的，通过 gRPC 把数据上报给后端。我主要做客户端调用侧 ——
> 连接复用、超时传递、失败重试这些是我踩过的。服务端实现是同事做的。」
> → 真实、有细节、且诚实划出了职责边界（**承认边界反而显得可信**）

**话术 B（若采集没用 gRPC）**：
> 「工作里的采集服务我用 Go 写并发和数据处理，RPC 这块用得浅。
> **但我自己做的项目里用 gRPC 做了服务拆分**，proto 定义、拦截器、超时传递我都踩过。」

**为什么这两个话术都比编造强**：
1. 真实工作经历（18 个月 Go 并发）
2. 主动学习（6 周内做出可演示项目）
3. **诚实的职责边界**（面试官最反感的是含糊其辞，最认可的是清楚的边界感）

---

## 5. 接下来要做的

### 阻塞项（必须先解决）

| # | 事项 | 谁做 |
| :--- | :--- | :--- |
| 1 | 回忆并确认 §2-C 的 5 项待确认信息（todo、带人、独立开发、7年、并发度） | 你 |
| 2 | 拆 10 份 JD 出词频表 | 你（9/19 上午） |
| 3 | 按本方案改写 HTML 模板 | 你（9/19 下午） |

### 风险提示

**如果某一条实在想不起来**：**删掉它，不要填估算值**。
5 年经验的简历，**少一条不致命，多一条假的致命**。

### 一个额外收益

你在回填监控项目的过程中会发现：**「并发度怎么定的」「超时怎么设的」这些问题的答案，正好就是 `P0-知识清单.md` ⑥⑦ 要学的内容。**

把学习和真实项目挂钩，比孤立学八股效率高得多 —— 而且学完就是你的，不用"挂"。
